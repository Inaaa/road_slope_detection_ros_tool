# File was automatically generated by mrt_cmake_modules
#
# This file exports the following variables:
# road_slope_detection_ros_tool_LIBRARIES: Library targets that should be linked against
# road_slope_detection_ros_tool_EXPORTED_TARGETS: As required by catkin
# road_slope_detection_ros_tool_EXPORTS_TARGETS: To indicate this package exports targets instead of plain libraries


####### Expanded from @PACKAGE_INIT@ by configure_package_config_file() #######
####### Any changes to this file will be overwritten by the next CMake run ####
####### The input file was packageConfig.cmake.in                            ########

get_filename_component(PACKAGE_PREFIX_DIR "${CMAKE_CURRENT_LIST_DIR}/../../../" ABSOLUTE)

####################################################################################

set(road_slope_detection_ros_tool_LIBRARIES road_slope_detection_ros_tool::road_slope_detection_ros_tool-ring_keeper-nodelet;road_slope_detection_ros_tool::road_slope_detection_ros_tool-road_rebuild-nodelet;road_slope_detection_ros_tool::road_slope_detection_ros_tool-surface-nodelet;road_slope_detection_ros_tool::road_slope_detection_ros_tool_compiler_flags)  # things that dependend packages should link against

# catkin wants this. Don't bother with it.
set(road_slope_detection_ros_tool_EXPORTED_TARGETS)

# mark this as a catkin project
set(road_slope_detection_ros_tool_FOUND_CATKIN_PROJECT TRUE)
set(road_slope_detection_ros_tool_EXPORTS_TARGETS TRUE)

macro(_init_package_dependencies)
    # gets all targets that this package export_depends on and sets var to the name of these targets.
    # because this config file might be used recursively we have to be extra careful that variables set here do not affect each other
    if(TARGET road_slope_detection_ros_tool::auto_deps_export)
        return()
    endif()
    if(NOT EXISTS "${CMAKE_CURRENT_LIST_DIR}/auto_dep_vars.cmake")
        message(FATAL_ERROR "Project road_slope_detection_ros_tool did not export its dependencies properly! No auto_dep file was found.")
    endif()
    # Resolve deps in the same style they were resolved when this project was built
    include("${CMAKE_CURRENT_LIST_DIR}/auto_dep_vars.cmake") # sets _road_slope_detection_ros_tool_EXPORT_PACKAGES_

    set(road_slope_detection_ros_toolAutoDeps_PREFIX "road_slope_detection_ros_tool")
    set(road_slope_detection_ros_toolAutoDeps_NO_CATKIN_EXPORT TRUE) # disable variable export because catkin is not listening here
    if(NOT mrt_cmake_modules_FOUND)
        # We need the mrt_cmake_modules because they provide extra CMake Modules for finding thirdparty stuff
        find_package(mrt_cmake_modules REQUIRED)
    endif()
    find_package(road_slope_detection_ros_toolAutoDeps REQUIRED
        COMPONENTS ${_road_slope_detection_ros_tool_EXPORT_PACKAGES_}
        PATHS ${CMAKE_CURRENT_LIST_DIR} NO_DEFAULT_PATH
        )
    if(NOT OpenMP_FOUND)
        find_package(OpenMP) # openmp is by default always a dependency
    endif()
    unset(road_slope_detection_ros_toolAutoDeps_PREFIX)
    unset(road_slope_detection_ros_toolAutoDeps_NO_CATKIN_EXPORT)
endmacro()

if(road_slope_detection_ros_tool_LIBRARIES)
    _init_package_dependencies()
endif()

# add the targets
if(EXISTS "${CMAKE_CURRENT_LIST_DIR}/road_slope_detection_ros_toolTargets.cmake")
    include("${CMAKE_CURRENT_LIST_DIR}/road_slope_detection_ros_toolTargets.cmake")
endif()

# add dependencies to other things generated by this package
set(road_slope_detection_ros_tool_targets road_slope_detection_ros_tool-ring_keeper-exec;road_slope_detection_ros_tool-road_rebuild-exec;road_slope_detection_ros_tool-surface-exec;road_slope_detection_ros_tool_geninterface;road_slope_detection_ros_tool_gencfg)
if(road_slope_detection_ros_tool_targets AND NOT TARGET road_slope_detection_ros_tool::mrt_exported_targets)
    set(namespaced_exported_targets)
    foreach(t ${road_slope_detection_ros_tool_targets})
        if(NOT TARGET ${target})
            add_library(road_slope_detection_ros_tool::${t} INTERFACE IMPORTED)
            list(APPEND namespaced_exported_targets road_slope_detection_ros_tool::${t})
        else()
            list(APPEND namespaced_exported_targets ${t})
        endif()
    endforeach()
    add_library(road_slope_detection_ros_tool::mrt_exported_targets INTERFACE IMPORTED)
    add_dependencies(road_slope_detection_ros_tool::mrt_exported_targets ${namespaced_exported_targets})
    list(APPEND road_slope_detection_ros_tool_LIBRARIES road_slope_detection_ros_tool::mrt_exported_targets)
    unset(namespaced_exported_targets)
endif()

set(road_slope_detection_ros_tool_EXTRAS_FILE )
if(road_slope_detection_ros_tool_EXTRAS_FILE)
    include(${CMAKE_CURRENT_LIST_DIR}/${road_slope_detection_ros_tool_EXTRAS_FILE})
endif()
unset(road_slope_detection_ros_tool_EXTRAS_FILE)
unset(road_slope_detection_ros_tool_targets)
